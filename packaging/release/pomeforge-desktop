#!/bin/sh
set -eu
entry=$(readlink -f -- "$0")
root=$(CDPATH= cd -- "$(dirname -- "$entry")/.." && pwd -P)
mkdir -p -- "$HOME/PomeforgeProjects"
exec "$root/bin/pomeforge" app --open --workspace "$HOME/PomeforgeProjects" --listen 127.0.0.1:0
