# Verification record

Integration and review are in progress. The records below distinguish actual execution from the final composed application's pending checks. All compilation, asset processing, signing, tests, and browser automation described here ran in Linux containers. The workstation orchestrated those containers and edited source; it did not compile iOS code or run Xcode.

| Check | Source and environment | Result |
| --- | --- | --- |
| CLI and graphical workspace baseline | Orchard `3ad6596e00e37ac32e20f2c94997e9abdb94af8a`; Linux arm64 | 75 Go test pass events, vet, arm64/amd64 builds, live Chromium project creation and blocked-build flow, authentication/Origin rejection, narrow viewport, screenshots and video |
| Arch executable | Same baseline; pinned Arch Linux amd64 container with CPU emulation | Actual `version`, project creation and prerequisite-plan execution; this is not an Omarchy desktop or device test |
| Managed dependency installer | Reviewed source `d2b5014a013e39705027f51f052101bb4af5c510` plus `81d046df9de9f6261da432008b4520bb1c714ded` | Actual pinned xtool, ASC and zsign installation, integrity/tamper checks, and process-cleanup regression checks passed |
| Asset compiler | Reviewed bridge at `d2b5014`; Swift 6.3.3 Linux arm64 | 11 wrapper tests and release build passed; 18 icon slots produced 15 distinct PNG files plus `Assets.car` |
| Apple SDK extraction and setup | Operator-supplied Xcode 26.6 archive; unxip 3.3.0, xtool 1.19.0 and Swift 6.3.3 Linux arm64 | Full XIP extraction, Darwin SDK construction, user-owned header staging and native `swift sdk install` passed; explicit XDG configuration isolated the SDK store |
| Real SwiftUI compilation | Project generated by Orchard `3ad6596`; actual iPhoneOS 26.5 SDK | `xtool dev build --configuration debug` compiled and linked the app in Linux in 39.91 seconds; actual arm64 Mach-O executable produced |
| Native IPA packaging | Distribution source `6b7d6f2abe5624d092dc66af5c19b716fb9aaae7`, under independent review | Real AssetKit and zsign 1.1.2 exported a Linux-linked synthetic Mach-O using a synthetic identity; all icon files, canonical archive permissions, profile checks and source preservation passed |
| Real SwiftUI app through IPA export | Same distribution source and the separate SDK-corrected real SwiftUI build | Read-only preflight, actual AssetKit and zsign, SDK-derived DT metadata, complete icons, canonical archive and source preservation passed with a synthetic identity |

The real SwiftUI executable SHA-256 is `5aab8aed8060ce98ca0626dcf58eb515812111565756b51374a6cd47ace001f8`. Its baseline bundle has iPhone/iPad families and minimum iOS 17, but no release icons or DT metadata. The initial Linux Clang invocation also recorded SDK 17 in `LC_BUILD_VERSION` despite using SDK 26.5. A separate full xtool rebuild verified that passing the same installed SDK through Clang's Darwin `-isysroot` option produces minimum iOS 17 and SDK 26.5 without inventing a version. Incorporating that verified correction into Orchard remains part of release integration.

The supplied Xcode archive SHA-256 is `06384762f286fb4d20440f7a67ba40cdf79bedb1cbee9e5111e3030a53a6cb81`. It reports Xcode 26.6/build 17F113, SDK version 26.5 and SDK system build 23F81a. This is operator-supplied provenance, not a claim that Orchard verified Apple's archive signature. No Apple SDK is included in this repository or in distributable images.

The synthetic native IPA SHA-256 is `c76b7a8820fd24f9988bc956159b0349002f9d3ed51c60a1c9edddfac0ca7633`. The private harness uses a PKCS#12 adapter only to carry its arbitrary test CA into zsign. It does not establish the production PEM identity path or Apple's trust. The receipt correctly reports Apple processing as `not_checked` and code-signature cryptographic verification as `not_performed`.

The real SwiftUI app's fixture-signed IPA SHA-256 is `8d6fc1607fb5cdaf448eca25ca9961a5d0c3296503089ce2d26bf47c6e742b8b`. It reports Mach-O SDK 26.5.0 and minimum iOS 17. Its metadata was derived from the retained real SDK records, while its profile, identity and icon artwork remain test fixtures. This exercised the distribution library through a private harness, not yet the final CLI release adapter. The same test-identity limits apply; this IPA is not offered as an installable or App Store-ready app.

Final composed CLI/GUI/container checks remain pending. Physical iPhone/iPad installation and interaction, a real Apple distribution identity, Apple upload processing, TestFlight and review submission have not been established. Unit tests, synthetic profiles and local structural inspection do not establish those results.

Source archives, actual app/IPA outputs, logs, screenshots and videos are retained privately outside the reusable repository. Further upstream evidence is in [compatibility.md](compatibility.md).
