# Complete the Linux workspace integration

Run this packet after the reviewed core, app, bootstrap, asset compiler and distribution library have been integrated. Read their actual public APIs and tests before editing. This packet connects existing working implementations; it must not duplicate their cryptographic parsers or download logic. Own CLI/core/server integration, template improvements, focused tests, and necessary UI controls. Keep all builds/tests in Linux. The parent owns the top-level README and final verification report.

## Tools and onboarding

Embed `toolchains.lock.json` once in a small root Go package; load it through `bootstrap.Load`. Prefer verified managed installs in XDG paths during tool discovery. Add `orchard tools install TOOL --json` to preview the actual pinned plan (version, bytes, destination, source, SHA), and `--execute` to install through bootstrap. Include all/individual choices only as supported by the package. Refuse non-Linux and unsupported architectures clearly. A missing or corrupt managed receipt must not count as available. Never change the global PATH or invoke sudo. Make setup discoverable from the graphical empty state, with actual installer plan/run and result handling, not a download-success simulation.

Swift 6.3+, compatible system libraries, USB service/permissions, and an operator-provided Apple SDK remain explicit prerequisites. Include distro-specific guidance for Arch/Omarchy, Debian/Ubuntu, Fedora and musl; do not claim untested distributions have passed. Provide a Linux command to compile the pinned AssetKit bridge and register its executable in user state. Check its version or executable integrity before use.

## SDK and identity setup

Implement a reviewed SDK import action using the verified xtool command contract. An extracted Xcode.app directory is acceptable input even though all extraction/build commands execute on Linux. XIP input may be supported when truthful metadata can be retained. Never point `xtool sdk build` at an existing output: it unconditionally deletes that output. Build into a newly reserved private directory, retain source hash/provenance, inspect real SDKSettings.plist/platform/Xcode version files, then install the resulting SDK through supported Swift/xtool commands. Record available fields; do not invent Xcode/build values. Explain missing source metadata precisely. Build capability requires actual SDK status, not just two binaries on PATH.

Pinned xtool 1.19.0 source clarifies that `--arch` accepts `arm64` or `x86_64` (not `aarch64`). `sdk build INPUT OUTPUT_PARENT` creates `OUTPUT_PARENT/darwin.xtoolsdk`. Actual non-root Linux execution established that upstream `sdk install` fails when copying root-owned Clang headers. Use the verified user-owned artifact-bundle staging and native `swift sdk install` path in `docs/linux-toolchain-decisions.md`, with an explicit selected XDG store. Orchard must refuse replacement and preserve existing SDKs. `sdk status` can exit zero while reporting `Not installed`; inspect its actual output. Retain Xcode/platform/SDK metadata from the fully extracted input before xtool filters it. Never infer missing values from a successful import.

Add `orchard signing` setup/inspection commands backed by distribution private configuration. Key/cert/profile/trust-root paths are local operator inputs, never their contents. Keep configuration outside tracked projects, omit private paths from public plans/results, reject encrypted identities without exposing passwords, and show certificate/profile/chain facts. A graphical user can select a named configured identity and see blockers; never expose key material over HTTP. Trust anchor provenance is explicit and is not synonymous with Apple approval.

## Native export and store operations

Connect the distribution package to a real export action, using AssetKit on staging before zsign and preserving original bundles. Accept explicit bundle/identity/output/catalog inputs through documented CLI flags and strict typed API fields. Keep unsigned build and distribution export separate. If there is no detected release bundle, show the actual prior build command. Do not permanently block export when valid prerequisites are supplied. Never substitute xtool's development signer for distribution signing.

Make the generated SwiftUI project practical for iPhone and iPad: library product, proper Info.plist family/version values, documented editable Swift source, and an icon catalog workflow. Provide deterministic PNG resizing/generation from an operator-supplied 1024px icon, or a clearly marked generated placeholder, with complete explicit iPhone/iPad/marketing sizes accepted by the supported AssetKit pipeline. No Xcode actool, ibtool, simulator, remote Mac, or Xcode-only build service.

Add IPA inspect command and show SHA256/identity/version/profile/SDK facts in plans. Upload must bind the exact IPA bytes to a single-use reviewed plan; changed files require a new plan. Validate and submit require real safe ASC app/version/build IDs, including UUIDs. ASC 5 upload/validate/review commands must match the recorded Linux help. Submit never follows upload automatically. Return actual exit status and bounded redacted output, including failed operation results. Explicit execution and external-write confirmation are separate controls. Per-tool credential environments remain restricted.

Update discoverable JSON schema/help and HTTP contract for every added field and action. The same operations must be usable by a human without AI and by an agent without an interactive prompt. Existing CLI/API tests, plan replay protections, nonregular-file protections, process-group cancellation, redaction and failed-result tests must continue to pass. Write meaningful new integration tests for installer activation/discovery, SDK output preservation/provenance, configured identity selection, export failure and unchanged sources, executable native plans, and refusal of unconfirmed upload/submit. Synthetic fixtures prove adapter behavior only; do not label them a real iOS build or Apple acceptance.

## Factory result contract

This is a local development packet with no Linear binding or external release authorization. Planning declares exactly one runtime target: `web` / `orchard-workspace`, with image and video required. For planning/build/review the configured proofPolicy is the string `none`, never null, with empty artifacts and proofRequests. Use the exact typed result/finding schema. The parent runs final exact-SHA Linux browser/CLI proof under a separate video policy. Commit the implementation, list checks actually run and outstanding external prerequisites, then obtain independent review.
